import { favoriteLinks, kpiCards, recentActivities } from "@/data/portalData";
import { NAV_ITEMS } from "./menu";
import { MODULE_CARDS } from "./modules";

export { MODULE_CARDS, NAV_ITEMS };
export const KPI_CARDS = kpiCards;
export const RECENT_ACTIVITIES = recentActivities;
export const FAVORITE_LINKS = favoriteLinks;
